//This is just for pipeline to pass, the type of the data will probably change based on login
type UserDataType = {
  username: string;
  id: string;
  token: boolean;
};
export default UserDataType;
